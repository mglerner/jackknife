{
  "name": "jackknife",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Jackknife — a mobile-web game that teaches you to back up a trailer.",
  "scripts": {
    "dev": "vite --host",
    "build": "tsc && vite build",
    "preview": "vite preview --host",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/three": "^0.184.1",
    "playwright": "^1.61.0",
    "typescript": "^5.7.3",
    "vite": "^6.0.7",
    "vitest": "^3.0.4"
  },
  "dependencies": {
    "three": "^0.184.0"
  }
}
