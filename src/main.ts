import "./styles.css";

// Placeholder bootstrap — replaced in step 12 once the engine is wired.
const app = document.querySelector<HTMLDivElement>("#app")!;
app.textContent = "Jackknife — loading…";
